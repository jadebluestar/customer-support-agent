# Next week

If I had another week, in priority order:

1. Relabel the golden set with a second annotator.
2. Bootstrap 95% CI on macro F1.
3. Retrieval recall@5 on more queries.
4. Replace TF-IDF retrieval with sentence embeddings only if recall@5 is poor.
5. Split `general_complaint` into named sub-categories.
6. Fix the LLM judge rubric so it separates decision-correctness from reply-quality.
7. Escalation threshold tuning on a validation set.
8. Temporal split (2016 → 2017).
9. Cross-brand generalisation.
10. Cache LLM responses keyed by prompt hash.